{
  "name": "valkompass-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:turbo": "next dev --turbopack",
    "dev": "bun run prepare-kb-documents && next dev",
    "build": "bun run prepare-kb-documents && next build",
    "start": "next start",
    "lint": "eslint \"src/**/*.{ts,tsx}\" \"test/**/*.ts\" \"*.config.*\"",
    "test": "vitest",
    "test:run": "vitest run",
    "test:ui": "vitest --ui",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage",
    "test:ci": "vitest run --coverage --reporter=default --reporter=junit",
    "prepare-kb-documents": "mkdir -p public/kb-documents && (cd knowledge-base/documents && find . -type d -exec mkdir -p ../../public/kb-documents/{} \\; && find . -name \"*.pdf\" -exec cp -p {} ../../public/kb-documents/{} \\; && cd ../..)",
    "fetch-politicians": "cd commands && bun install && bun run fetch-politicians",
    "extract-parties": "cd commands && bun install && bun run extract-parties",
    "test-fetch-votes": "cd commands && bun install && bun run test-fetch-votes",
    "performance-test": "cd commands && bun install && bun run performance-test",
    "concurrency-test": "cd commands && bun install && bun run concurrency-test",
    "fetch-votes": "cd commands && bun install && bun run fetch-votes",
    "test-fetch-voteringar": "cd commands && bun install && bun run test-fetch-voteringar",
    "fetch-voteringar": "cd commands && bun install && bun run fetch-voteringar",
    "enrich-voteringar": "cd commands && bun install && bun run enrich-voteringar",
    "test-enrich-voteringar": "cd commands && bun install && bun run test-enrich-voteringar"
  },
  "dependencies": {
    "@google/generative-ai": "0.24.1",
    "class-variance-authority": "0.7.1",
    "clsx": "2.1.1",
    "deepmerge-ts": "7.1.5",
    "dotenv": "17.4.2",
    "lucide-react": "1.17.0",
    "neo4j-driver": "6.0.1",
    "next": "16.2.6",
    "openai": "6.39.1",
    "posthog-node": "5.35.7",
    "react": "19.2.6",
    "react-dom": "19.2.6",
    "react-markdown": "10.1.0",
    "rehype-sanitize": "6.0.0",
    "tailwind-merge": "3.6.0",
    "uuid": "14.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "3.3.5",
    "@tailwindcss/postcss": "4.3.0",
    "@testing-library/jest-dom": "6.9.1",
    "@testing-library/react": "16.3.2",
    "@types/node": "25.9.1",
    "@types/react": "19.2.15",
    "@types/react-dom": "19.2.3",
    "@vitest/coverage-v8": "4.1.7",
    "@vitest/ui": "4.1.7",
    "eslint": "9.39.4",
    "eslint-config-next": "16.2.6",
    "jsdom": "29.1.1",
    "tailwindcss": "4.3.0",
    "tw-animate-css": "1.4.0",
    "typescript": "6.0.3",
    "vitest": "4.1.7"
  }
}
